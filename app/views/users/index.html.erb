<p id="notice"><%= notice %></p>

<h1>I don't even care about users.</h1>

<div>
  <%  @users.each do |user| %>
      <div>See posts for: <%= link_to user.username, user_posts_path(user.id) %></div>
  <%  end  %>
</div>

<%= link_to 'Back', :back %>
